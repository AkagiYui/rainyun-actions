{
  "name": "rainyun-actions",
  "version": "1.0.0",
  "private": true,
  "description": "雨云 (Rainyun) GitHub Actions — 通过 GitHub Actions 管理雨云资源",
  "scripts": {
    "build": "ncc build src/restart-app.ts -o dist/restart-app",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@actions/core": "^1.11.1"
  },
  "devDependencies": {
    "@types/node": "^24",
    "@vercel/ncc": "^0.38.3",
    "typescript": "^6"
  }
}
